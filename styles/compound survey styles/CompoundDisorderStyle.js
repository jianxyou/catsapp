import { StyleSheet } from 'react-native';

export default CompoundDisorderStyle = StyleSheet.create({

    subheader: {
        fontSize: 35,
        marginBottom: 15,
        marginTop: 30,
        textDecorationLine: 'underline',
    },

    surveyunit: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'flex-start',
        
    },

    subheadercontainer: {
        marginLeft: 80,
        marginTop: 20,
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'flex-start',
    },

})
