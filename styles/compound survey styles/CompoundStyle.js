import { StyleSheet } from 'react-native';

const CompoundStyle = StyleSheet.create({

    subheader: {
        fontSize: 25,
        marginBottom: 15,
        marginTop: 30,
        textDecorationLine: 'underline',
    }

})

export default CompoundStyle