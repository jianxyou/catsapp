import { StyleSheet } from 'react-native';

export default CompoundSDSStyle = StyleSheet.create({

    subheader: {
        fontSize: 25,
        marginBottom: 15,
        marginTop: 30,
        fontWeight: 'bold'
    },

    subheadercontainer: {
        marginBottom: 20,
    },

    surveyunit: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignContent: 'center',
        marginLeft: 80,
    },

})